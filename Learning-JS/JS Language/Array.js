// Arrays
let books = ["book1", "book2", "book3"];

// Interating through the list of books
for(i in books){
    console.log(books[i]);
}

// Getting a book by index
console.log(books[0]);

// Calling funtions inside books array
console.log(books.length);